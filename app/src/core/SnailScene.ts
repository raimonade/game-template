export default abstract class SnailScene extends PIXI.Container {
    // public resize(width:number, height:number) {
    //     this.gameWidth = width;
    //     this.gameHeight = height;
    // }
}